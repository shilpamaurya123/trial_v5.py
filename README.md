# trial_v5.py
trial_v5.py is the main application script for an IoT-based concrete admixture dosing system. It reads sensor data (pressure, tank level, flow, RPM), determines truck states (idle, loading, transit, unloading), executes automatic and manual dosing logic, and publishes telemetry data to the cloud via MQTT. T
